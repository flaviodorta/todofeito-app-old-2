// import { Country } from '../helpers/types';

// export const fetchCountries = async () => {
//   try {
//     const countries: Country[] = await fetch(
//       'https://restcountries.com/v3.1/all'
//     ).then((res) => res.json());
//     return countries.map((country: Country) => country.name.common);
//   } catch (err) {
//     return err;
//   }
// };

export {};
